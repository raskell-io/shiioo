apiVersion: v1
kind: Secret
metadata:
  name: shiioo-secrets
  namespace: shiioo
  labels:
    app.kubernetes.io/name: shiioo
type: Opaque
stringData:
  # Generate with: openssl rand -base64 32 | head -c 32
  # IMPORTANT: Replace this with your own key before deploying!
  encryption-key: "REPLACE_WITH_32_BYTE_KEY_HERE!!"
